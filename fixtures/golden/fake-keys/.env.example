# Example environment for the notifier demo — values are intentionally empty.
# Fill these in locally; real deployments source them from a secret manager.
# The hardcoded fake keys for this fixture live in src/ (see manifest.json).

OPENAI_API_KEY=
STRIPE_SECRET_KEY=
AWS_ACCESS_KEY_ID=
